import 'package:flutter/material.dart';

class FinancementScreen extends StatelessWidget {
  const FinancementScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Financement')),
      body: const Center(child: Text('Écran financement - à construire')),
    );
  }
}